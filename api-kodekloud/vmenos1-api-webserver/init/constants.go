package main

const DBName = "learning"
const DBUser = "root"
const DBPassword = "admin"

// Para não precisar instalar o mysql na máquina, use o docker com esses comandos
// docker run --name mysql -e MYSQL_ROOT_PASSWORD=admin -e MYSQL_DATABASE=learning -p 3306:3306 -d mysql:latest
