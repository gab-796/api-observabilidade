# ========================================
# GLOBAL CONFIGURATION
# ========================================
global:
  namespace: api-app-go
  environment: development

# ========================================
# COMMON LABELS AND ANNOTATIONS
# ========================================
commonLabels:
  project: api-observabilidade
  team: platform
  version: v1.0.0

commonAnnotations: {}

# ========================================
# SERVICE ACCOUNT CONFIGURATION
# ========================================
serviceAccount:
  create: true
  annotations: {}
  name: ""
  automount: true

# ========================================
# SERVICES CONFIGURATION
# ========================================
services:
  type: ClusterIP

# ========================================
# MONITORING AND HEALTH CHECKS
# ========================================
monitoring:
  healthChecks:
    enabled: true
    readinessProbe:
      initialDelaySeconds: 15
      periodSeconds: 10
      timeoutSeconds: 5
      failureThreshold: 3
      successThreshold: 1
    livenessProbe:
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 5
      failureThreshold: 3
      successThreshold: 1

# ========================================
# MYSQL DATABASE CONFIGURATION
# ========================================
mysql:
  enabled: true
  name: mysql
  replicaCount: 1
  port: 3306

  image:
    repository: mysql
    tag: "8.0"
    pullPolicy: IfNotPresent

  config:
    rootPassword: admin
    database: inventory
    user: root

  resources:
    limits:
      memory: "1Gi"
      cpu: "1000m"
    requests:
      memory: "512Mi"
      cpu: "500m"

# ========================================
# INVENTORY APPLICATION CONFIGURATION
# ========================================
inventoryApp:
  enabled: true
  name: inventory-app
  replicaCount: 1
  image:
    repository: gab796/inventory_app
    tag: v4.0
    pullPolicy: IfNotPresent
  ingress:
    enabled: true
    ingressClassName: nginx
    host: inventory.local
    exposeMetrics: true
    annotations: {}
    tls: false
  ports:
    http: 10000
    metrics: 2113

  # Application configuration
  config:
    logLevel: info
    ginMode: release
    additionalEnvVars: {}

  # Observability configuration
  observability:
    otel:
      protocol: grpc
    prometheus:
      metricsPath: /metrics

  # Secrets
  secret:
    dbPassword: "admin"  # Em produção, use Sealed Secrets ou External Secrets

  env: {}

  resources:
    limits:
      memory: "512Mi"
      cpu: "500m"
    requests:
      memory: "256Mi"
      cpu: "250m"

  # Autoscaling configuration
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 6
    targetCPUUtilizationPercentage: 50
    targetMemoryUtilizationPercentage: 80

# ========================================
# GRAFANA CONFIGURATION
# ========================================
grafana:
  enabled: true
  name: grafana
  replicaCount: 1

  image:
    repository: grafana/grafana
    tag: "11.6.0"
    pullPolicy: IfNotPresent

  ports:
    http: 3000

  config:
    # ⚠️ ESCOLHA: Autenticação anônima OU login tradicional

    # OPÇÃO 1: Autenticação anônima (como seu arquivo original)
    auth:
      anonymous:
        enabled: true
        orgRole: "Admin"
        disableLoginForm: true
        disableSignoutMenu: true

    # OPÇÃO 2: Login tradicional (comentar se usar anônimo)
    # adminUser: admin
    # adminPassword: admin123

    # Configurações gerais
    logLevel: warn
    rootUrl: "http://grafana-web.local"

    users:
      allowSignUp: false

    # Features e plugins
    featureToggles: "traceqlEditor,metricsSummary"

    plugins:
      install: "https://storage.googleapis.com/integration-artifacts/grafana-exploretraces-app/grafana-exploretraces-app-latest.zip;grafana-traces-app"

  # Data sources configuration
  datasources:
    tempo:
      enabled: true
      url: http://tempo:3200
      isDefault: true

    loki:
      enabled: true
      url: http://loki:3100
      maxLines: 1000

    pyroscope:
      enabled: true
      url: http://pyroscope:4040

    mimir:
      enabled: true
      url: http://mimir:9009/prometheus

# ========================================
# GRAFANA INGRESS CONFIGURATION
# ========================================

  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - host: grafana-web.local
        paths:
          - path: /
            pathType: Prefix
            service:
              name: grafana
              port: 3000
    exposeStack: false
    annotations: {}
    tls: []

  # Dashboards configuration
  dashboards:
    inventory:
      enabled: true
      title: "API Inventory - Observability Dashboard"
      folder: "API Inventory"

  resources:
    limits:
      memory: "512Mi"
      cpu: "500m"
    requests:
      memory: "256Mi"
      cpu: "250m"

  # Persistence (para manter dashboards, plugins instalados, etc.)
  persistence:
    enabled: false
    accessModes:
      - ReadWriteOnce
    size: 5Gi
    storageClass: ""  # Use uma StorageClass válida do cluster ou deixe vazio

  # Modo efêmero (dev): garante que o banco (grafana.db) seja recriado a cada start
  ephemeral:
    enabled: true      # Se true, ignora PVC (usa emptyDir) e roda initContainer limpando grafana.db
    resetDb: true      # Se false, apenas não persiste; se true força remoção explícita do arquivo

# ========================================
# GRAFANA ALLOY CONFIGURATION
# ========================================

alloy:
  enabled: true
  name: alloy
  replicaCount: 1
  stabilityLevel: "public-preview" # Permite usar o pyroscope.receive_http
  
  # Tipo de deployment: "deployment" ou "daemonset"
  # - deployment: Para coleta centralizada (padrão)
  # - daemonset: Para coleta por node (logs de host, node metrics, etc.)
  deploymentType: "deployment"

  # Configurações específicas para DaemonSet
  daemonset:
    hostNetwork: false  # Set true para acessar métricas de host
    hostPID: false      # Set true para coletar métricas de processo
    privileged: false   # Set true para acesso completo ao sistema host
    dnsPolicy: "ClusterFirst"  # ClusterFirstWithHostNet se hostNetwork=true
    
    # Node selector para DaemonSet
    nodeSelector: {}
    # Exemplo:
    # nodeSelector:
    #   kubernetes.io/os: linux
    
    # Tolerations para rodar em nodes específicos (ex: master nodes)
    tolerations: []
    # Exemplo:
    # tolerations:
    # - key: node-role.kubernetes.io/master
    #   operator: Exists
    #   effect: NoSchedule
    
    # Volumes de host para coleta de métricas de sistema
    hostVolumes:
      proc:
        enabled: false  # /proc para métricas de CPU, memória, etc.
      sys:
        enabled: false  # /sys para métricas de sistema
      rootfs:
        enabled: false  # / para métricas de filesystem

  image:
    repository: grafana/alloy
    tag: v1.9.1
    pullPolicy: IfNotPresent

  ports:
    http: 12345
    profiling: 4040   # Porta separada para o receiver pyroscope (evita conflito com HTTP interno do Alloy)

  config:
    logging:
      level: info
      format: json

    logs:
      allNamespaces: true  # Se true, coleta logs de todos os namespaces do cluster

  securityContext:
    runAsNonRoot: true
    runAsUser: 473
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL

  resources:
    limits:
      memory: "256Mi"
      cpu: "200m"
    requests:
      memory: "128Mi"
      cpu: "100m"

# ========================================
# OPENTELEMETRY COLLECTOR CONFIGURATION
# ========================================
otelCollector:
  enabled: true
  name: otel-collector
  replicaCount: 1

  image:
    repository: otel/opentelemetry-collector-contrib
    tag: "0.123.0-amd64"
    pullPolicy: IfNotPresent

  ports:
    otlpGrpc: 4317
    otlpHttp: 4318
    healthCheck: 13133

  config:
    logging:
      level: warn

    prometheus:
      scrapeInterval: 15s
      scrapeMimir: true
      mimirScrapeInterval: 30s

      # Kube State Metrics configuration
      scrapeKubeStateMetrics: true
      kubeStateMetrics:
        scrapeInterval: 30s
        metricsPath: /metrics
        namespaces: ["ksm"]  # Namespace onde o kube-state-metrics está instalado

    datadog:
      enabled: false

  resources:
    limits:
      memory: "1Gi"
      cpu: "1000m"
    requests:
      memory: "128Mi"
      cpu: "500m"

# ========================================
# GRAFANA TEMPO CONFIGURATION (TRACES)
# ========================================
tempo:
  enabled: true
  name: tempo
  replicaCount: 1

  image:
    repository: grafana/tempo
    tag: "2.6.0"
    pullPolicy: IfNotPresent

  ports:
    http: 3200
    otlpGrpc: 4317
    otlpHttp: 4318
    grpc: 9095

  config:
    server:
      logLevel: warn
    storage:
      trace:
        backend: local
        local:
          path: /var/tempo

  resources:
    limits:
      memory: "1Gi"
      cpu: "1000m"
    requests:
      memory: "512Mi"
      cpu: "500m"

# ========================================
# GRAFANA LOKI CONFIGURATION (LOGS)
# ========================================
loki:
  enabled: true
  name: loki
  replicaCount: 1

  image:
    repository: grafana/loki
    tag: "2.9.5"
    pullPolicy: IfNotPresent

  ports:
    http: 3100
    grpc: 9095

  config:
    authEnabled: false
    server:
      logLevel: warn
    storage:
      type: filesystem
      filesystem:
        chunksDirectory: /loki/chunks
        rulesDirectory: /loki/rules

  # Configuração de persistência específica para Loki
  persistence:
    enabled: false
    storageClass: ""
    accessModes:
      - ReadWriteOnce
    size: 1Gi

  resources:
    limits:
      memory: "512Mi"
      cpu: "500m"
    requests:
      memory: "256Mi"
      cpu: "250m"

# ========================================
# GRAFANA MIMIR CONFIGURATION (METRICS)
# ========================================
mimir:
  enabled: true
  name: mimir
  replicaCount: 1

  image:
    repository: grafana/mimir
    tag: "2.13.0"
    pullPolicy: IfNotPresent

  ports:
    http: 8080
    grpc: 9095

  config:
    multitenancyEnabled: false
    internalMetricsPort: 9009
    remoteWritePort: 9009
    server:
      logLevel: warn
    storage:
      backend: filesystem
      filesystem:
        dir: /data/blocks
        rulerDir: /data/ruler
    ingester:
      replicationFactor: 1
      outOfOrderTimeWindow: 10m
    limits:
      maxGlobalSeriesPerUser: 10000000
      ingestionRate: 100000
      outOfOrderTimeWindow: 10m

  resources:
    limits:
      memory: "2Gi"
      cpu: "1000m"
    requests:
      memory: "1Gi"
      cpu: "500m"

# ========================================
# GRAFANA PYROSCOPE CONFIGURATION (PROFILING)
# ========================================
pyroscope:
  enabled: true
  name: pyroscope
  replicaCount: 1

  image:
    repository: grafana/pyroscope
    tag: "1.13.0"
    pullPolicy: IfNotPresent

  ports:
    http: 4040
    grpc: 4041

  config:
    server:
      logLevel: warn
    storage:
      backend: filesystem
      filesystem:
        path: /var/lib/pyroscope

  resources:
    limits:
      memory: "1Gi"
      cpu: "1000m"
    requests:
      memory: "512Mi"
      cpu: "500m"

# ========================================
# PERSISTENCE (OPCIONAL)
# ========================================
persistence:
  enabled: false
  storageClass: ""
  accessModes:
    - ReadWriteOnce
  size:
    mysql: 1Gi
    grafana: 1Gi
    loki: 1Gi
    mimir: 1Gi
    tempo: 1Gi
    pyroscope: 1Gi

# ========================================
# SECURITY CONFIGURATION
# ========================================
podSecurityContext:
  fsGroup: 2000

securityContext:
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: false
  runAsNonRoot: true
  runAsUser: 1000

# ========================================
# NODE SELECTOR, AFFINITY, TOLERATIONS
# ========================================
nodeSelector: {}

tolerations: []

affinity: {}

# ========================================
# AUTOSCALING (OPCIONAL)
# ========================================
autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 50
  targetMemoryUtilizationPercentage: 80

# ========================================
# ADDITIONAL CONFIGURATION
# ========================================
nameOverride: ""
fullnameOverride: ""

# Extra environment variables for all deployments
extraEnvVars: []

# Extra volumes and volume mounts for all deployments
extraVolumes: []
extraVolumeMounts: []

# Pod annotations
podAnnotations: {}

# Deployment strategy
deploymentStrategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 1
    maxSurge: 1